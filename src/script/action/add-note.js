const form = document.querySelector('form');
const titleInput = form.elements.title;
const output = document.querySelector('p#out');

form.addEventListener('submit', (event) => {
  event.preventDefault();
  
});